<Window x:Class="RyCookieText.Views.SnippetEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Snippet"
        Height="450" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Keyword -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Keyword *" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <TextBox x:Name="KeywordBox"
                     Style="{StaticResource ModernTextBox}"
                     MaxLength="50"/>
            <TextBlock Text="The text you type to trigger this snippet"
                       FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Group -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Group" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <ComboBox x:Name="GroupBox"
                      IsEditable="True"
                      Height="36"
                      Padding="8"
                      FontSize="14">
                <ComboBoxItem Content="Common"/>
                <ComboBoxItem Content="Personal"/>
                <ComboBoxItem Content="Email"/>
                <ComboBoxItem Content="Code"/>
                <ComboBoxItem Content="Other"/>
            </ComboBox>
        </StackPanel>

        <!-- Enabled -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <CheckBox x:Name="EnabledCheckBox"
                      Content="Enabled"
                      IsChecked="True"
                      FontSize="14"/>
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Content *" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            <Border BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Background="White">
                <TextBox x:Name="ContentBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         Padding="8"
                         FontSize="14"
                         FontFamily="Consolas"/>
            </Border>
            <TextBlock Text="The text that will replace the keyword (supports multi-line)"
                       FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Validation Message -->
        <TextBlock x:Name="ValidationMessage"
                   Grid.Row="4"
                   Foreground="#F44336"
                   FontSize="12"
                   Margin="0,0,0,16"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="100"
                    Height="36"
                    Click="CancelButton_Click"
                    Background="{StaticResource TextSecondaryBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,12,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Save"
                    Width="100"
                    Height="36"
                    Click="SaveButton_Click"
                    Style="{StaticResource ModernButton}"/>
        </StackPanel>
    </Grid>
</Window>
